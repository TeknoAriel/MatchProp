{
  "functions": {
    "api/[[...path]].ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/$1" }
  ],
  "buildCommand": "cd ../.. && NPM_CONFIG_PRODUCTION=false pnpm install && pnpm build:shared && pnpm --filter api exec prisma generate -- --schema=./prisma/schema.prisma && pnpm --filter api build"
}
