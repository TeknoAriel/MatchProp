{
  "functions": {
    "api/handler.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/handler" }
  ],
  "buildCommand": "cd ../.. && NPM_CONFIG_PRODUCTION=false pnpm install && pnpm build:shared && pnpm --filter api prisma:generate && pnpm --filter api build"
}
